# ArchWright Rulebook v0.1.0
# Each entry is one audit rule. See docs for schema.

- id: R-04
  name: unbounded-tool-retry-loop
  pillar: Reliability
  severity: warning
  rationale: >
    The agent calls the same tool repeatedly with near-identical arguments and
    receives no new information, expending time and tokens without making
    progress. Such loops rarely end in success — they end in a timeout, a wrong
    answer, or a late escalation the agent could have made far earlier — while
    the user waits blindly, assuming work is happening.
  source: "Anthropic, 'Building Effective Agents' (2025) — loop-bounding; corroborated in production."
  detection: |
    calls = tool_calls in session, in order
    # GROUP consecutive same-tool calls into runs
    runs = split calls into maximal runs where run[i].name == run[i-1].name
    for run in runs:
      if len(run) < thresholds.max_repeats:          continue   # too short
      if config.tools[run.name].polling is true:     continue   # designed to repeat
      if mean_consecutive_arg_similarity(run) < thresholds.arg_similarity: continue  # exploration
      if all outputs in run are identical:
        emit Finding(rule=R-04, tool=run.name, count=len(run), evidence=run)
  evidence_threshold:
    type: structural
    min_occurrences: 1
  thresholds:
    max_repeats: 3
    arg_similarity: 0.9
  severity_rules:
    - "if session ends in escalation or error: critical"
    - "else: warning"
  inputs_required:
    traces: true
    config: optional
  limitations: >
    Detects only consecutive loops; interleaved patterns (A,B,A,B) are out of
    scope for v1. Relies on an arg-similarity function whose quality affects
    precision.
  remediation: >
    Add an iteration cap (<=3) with argument-similarity dedup in the agent graph;
    on cap, branch to query reformulation or escalation. Annotate genuine polling
    tools as polling:true to suppress false positives.

- id: H-02
  name: context-bloat
  pillar: Context Hygiene
  severity: warning
  rationale: >
    The agent resends its whole history every turn, so input tokens climb —
    wasting money and degrading answer quality as the context window fills.
  source: "General context-management practice"
  detection: |
    tokens = [input_tokens of each LLM call in session, in order]
    if length(tokens) < thresholds.min_steps: skip     # too short to judge a trend
    climbing_ratio = fraction of steps where tokens[i] > tokens[i-1]
    growth = tokens.last / tokens.first
    if climbing_ratio >= thresholds.climbing_ratio and growth >= thresholds.growth_warning:
        emit Finding(rule="H-02", session=session.id,
                    evidence=tokens, growth=growth,
                    severity = warning if growth < thresholds.growth_critical else critical)
  evidence_threshold:
    type: statistical
    min_sessions: 30
    min_prevalence: 0.30
  thresholds:
    min_steps: 5
    climbing_ratio: 0.9
    growth_warning: 3.0
    growth_critical: 5.0
  severity_rules:
    - "if growth >= growth_critical (5.0): critical"
    - "else: warning"
  inputs_required:
    traces: true
    config: false
  limitations: >
    A genuinely long, complex task can look like bloat (false positive), and the
    rule cannot tell good accumulation (relevant context) from lazy accumulation
    (dumping everything).
  remediation: >
    Summarize tool outputs after use; use a sliding-window or running-summary
    memory instead of resending full history; trim stale turns.

- id: C-01
  name: model-tier-mismatch
  pillar: Cost Efficiency
  severity: warning
  rationale: >
    An expensive frontier model is doing work a small model could handle — e.g.
    a classify_intent step running on a frontier model but consistently producing
    ~20-token answers — inflating cost with no quality benefit.
  source: "Efficient model selection practice - Token Usage"
  detection: |
    calls = [(step_name, model, output_tokens) for each LLM call]
    groups = bucket calls by step_name
    for step, group in groups:
      if len(group) < thresholds.min_calls_per_step: continue
      if not price_catalog[group.model].is_frontier_tier: continue
      if p95(group.output_tokens) >= thresholds.output_tokens_p95: continue
      # convicted: expensive model, consistently short outputs
      saving = len(group) * (price_catalog[model].cost - price_catalog.cheapest.cost)
      emit Finding(rule="C-01", step=step, model=model,
                   p95_output=p95(group.output_tokens), calls=len(group),
                   projected_saving=saving)
  evidence_threshold:
    type: statistical
    min_calls_per_step: 30
  thresholds:
    output_tokens_p95: 50
    min_calls_per_step: 30
  severity_rules:
    - "if projected_saving >= 100/mo: critical"
    - "else: warning"
  inputs_required:
    traces: true
    config: false
    price_catalog: true
  limitations: >
    Flagging a short-output step identifies a cost opportunity, not proof that a
    smaller model preserves accuracy — some superficially trivial steps rely on
    the larger model's quality. Cannot see output quality, only length.
  remediation: >
    Route this step to a smaller model tier, then re-run the task's evaluation
    suite before switching in production — a cheaper model can silently reduce
    accuracy on tasks that appear trivial by output length.

- id: S-02 
  name: untrusted-output-in-system-prompt
  pillar: Safety
  severity: critical
  rationale: >
    Untrusted, externally-authored tool output (email, web, customer text) is concatenated into a later LLM call's system prompt, 
    letting an attacker's words be read as instructions — a prompt-injection path to any tool the agent holds
  source: "OWASP LLM Top 10 — LLM01 Prompt Injection"
  detection: |
    untrusted_outputs = [output of tool_call for each tool_call
                         where config.tools[name].untrusted is true]
    for each llm_call in session, in order:
      sysprompt = llm_call.system_prompt
      for each prior_output in untrusted_outputs seen before this call:
        if sysprompt contains prior_output (overlap >= thresholds.min_match_length):
          emit Finding(rule="S-02",
                       source=prior_output.step,   # where untrusted text entered
                       sink=llm_call.step,          # where it wrongly landed
                       severity=critical)
  evidence_threshold:
    type: structural
    min_occurrences: 1
  thresholds:
    min_match_length: 3
  inputs_required:
    traces: true
    config: optional
  limitations: >
    exact-substring matching catches verbatim flows but misses paraphrased/summarized untrusted content.
  remediation: >
    Move untrusted output into the user/content role with explicit delimiters, 
    never the system prompt; add an injection-classifier before the LLM call.
    
- id: H-07 
  name: ambiguous-tool-routing
  pillar: Reliability
  severity: warning
  rationale: >
    The agent misroutes request to a functionally different, but similarly named tool.
  source: "Tool naming or configuration Anti-pattern"
  detection: |
    # SIGNAL 1 (config, structural): confusable definitions
    for each pair (A, B) of tools in config:
      if description_similarity(A, B) >= thresholds.min_description_similarity:
        confusable_pairs.add((A, B))
    # SIGNAL 2 (traces, statistical): confusion actually firing
    for each confusable pair (A, B):
      count sessions where A and B are both called for the same intent,
        or A is called then B immediately retried for the same request
      if that count >= thresholds.min_misroute_sessions:
        emit Finding(rule="H-07", pair=(A,B), severity=critical)   # confirmed misrouting
      else:
        emit Finding(rule="H-07", pair=(A,B), severity=warning)    # latent risk only
  evidence_threshold:
    type: hybrid
    config_signal: structural      # 1 confusable pair is a real design flaw
    trace_signal: statistical      # need repeated misroutes to confirm it fires
    min_misroute_sessions: 5   
  thresholds:
    min_description_similarity: 0.85   # how alike two descriptions must be to flag
    min_misroute_sessions: 5           # trace occurrences to escalate to critical
  inputs_required:
    traces: optional
    config: true
  limitations: >
    description-similarity is heuristic — two legitimately-related tools (e.g. search_docs / search_web) may look confusable without actually misrouting; needs human review of flagged pairs. 
    Semantic similarity (embeddings) would beat string distance — a v2 improvement
  remediation: >
   make tool names and descriptions maximally distinct; add explicit "use this when… / do NOT use when…" disambiguation to each description; 
   consolidate genuinely-redundant tools; consider a routing eval set.
    
    
- id: S-05 
  name: ungated-high-authority-action
  pillar: Safety
  severity: critical
  rationale: >
    Granting WRITE or UPDATE ability to a tool that can be called by an LLM is a high-risk design pattern.
  source: "Generic security principle: least privilege and controlled access"
  detection: |
    # STEP 1: identify high-authority tools (config annotation or name heuristic)
    high_auth = [t for t in config.tools
                 if t.high_authority is true
                 or t.name matches /delete_|send_|pay_|refund_|update_|execute_/]
    # STEP 2 (structural): did any fire without a preceding human-approval gate?
    for each tool_call in session where tool_call.name in high_auth:
      if no human_approval / confirmation step precedes this call in the session:
        emit Finding(rule="S-05", tool=tool_call.name,
                     step=tool_call.step, severity=critical)
  evidence_threshold:
    type: structural
    min_occurrences: 1         
  thresholds:
    min_occurrences: 1 
  inputs_required:
    traces: optional
    config: true
  limitations: >
    Human-approval steps that happen out-of-band (in a UI or workflow the trace
    doesn't capture) are invisible here, so an absent gate-step is not always
    proof that no gate exists — flagged findings need human confirmation.
    Name-heuristics also miss dangerously-named-innocuously tools (e.g. a
    "finalize" or "submit" tool that charges a card), so config annotation of
    high-authority tools is strongly preferred over relying on the heuristic.
  remediation: >
    Require an explicit human-approval or confirmation step before any
    high-authority tool executes. Apply least privilege so the agent holds only
    the authority it genuinely needs. Where possible make irreversible actions
    reversible or idempotent (soft-delete, refundable holds) so an erroneous call
    can be undone. Annotate high-authority tools in config so detection does not
    depend on name heuristics.
    
- id: C-03
  name: uncached-repeated-prefix
  pillar: Cost Efficiency
  severity: warning
  rationale: >
    A large, identical prompt prefix (system prompt + tool schemas) is resent on
    nearly every LLM call without prompt caching, so the same thousands of tokens
    are billed at full input price on every call instead of the discounted cached
    rate.
  source: "Provider prompt-caching best practice"
  detection: |
    prefixes = [llm_call.prompt_prefix for each llm_call in session]
    common_prefix = longest prefix shared by >= thresholds.min_share_fraction of calls
    if length(common_prefix) < thresholds.min_prefix_tokens: skip   # too small to matter
    calls_sending_it = count of calls whose prompt starts with common_prefix
    if calls_sending_it >= thresholds.min_calls and cache not enabled on these calls:
      wasted = calls_sending_it * length(common_prefix) * (input_price - cached_price)
      emit Finding(rule="C-03", prefix_tokens=length(common_prefix),
                   calls=calls_sending_it, projected_saving=wasted, severity=warning)
  evidence_threshold:
    type: statistical
    min_calls: 20
  thresholds:
    min_prefix_tokens: 500       # prefix must be this large to be worth caching
    min_calls: 20                # repeated across at least this many calls
    min_share_fraction: 0.8      # prefix shared by >=80% of calls
  inputs_required:
    traces: true
    config: false
    price_catalog: true
  limitations: >
    Prompt-caching support and pricing vary by provider (some offer none, or
    require minimum prefix sizes), so projected savings are estimates. Detecting
    whether a call is already cached depends on the trace exposing cache-hit
    metadata, which not all frameworks emit.
  remediation: >
    Enable provider prompt-caching and mark the stable prefix (system prompt +
    tool schemas) as the cached segment. Keep cacheable content at the start of
    the prompt, since caching is prefix-based, and avoid injecting per-call
    variable content into the prefix that would break the cache.

- id: O-01
  name: insufficient-telemetry-coverage
  pillar: Observability
  severity: warning
  rationale: >
    The agent emits incomplete telemetry — spans missing token counts, unnamed or
    generic step names, tool calls with no spans, or calls without success/error
    status. Poor observability means failures and cost go unseen, and it disables
    several other audit rules that depend on this data (C-01, C-03, H-02).
  source: "OpenTelemetry GenAI semantic conventions; observability best practice"
  detection: |
    llm_spans  = all LLM-call spans in the trace corpus
    tool_spans = all tool-call spans in the trace corpus
    # each sub-check emits its OWN finding with its own coverage %
    # SUB-CHECK 1: missing token counts
    missing_tokens = fraction of llm_spans lacking input_tokens or output_tokens
    if missing_tokens > thresholds.max_missing_fraction:
      emit Finding(rule="O-01", check="missing_token_counts",
                   coverage=1 - missing_tokens, severity=warning)
    # SUB-CHECK 2: unnamed / generic step names
    generic = fraction of spans with blank name or a generic name (e.g. "llm_call", "step")
    if generic > thresholds.max_generic_fraction:
      emit Finding(rule="O-01", check="unnamed_steps",
                   coverage=1 - generic, severity=warning)
    # SUB-CHECK 3: tool calls with no status/error field
    no_status = fraction of tool_spans lacking a success/error/status attribute
    if no_status > thresholds.max_missing_fraction:
      emit Finding(rule="O-01", check="no_status_capture",
                   coverage=1 - no_status, severity=warning)
    # SUB-CHECK 4: untraced tool executions (needs config to know what SHOULD appear)
    if config available:
      declared_tools = config.tools
      seen_tools = distinct tool names in tool_spans
      untraced = declared_tools not in seen_tools
      if untraced is non-empty:
        emit Finding(rule="O-01", check="untraced_tools",
                     tools=untraced, severity=warning)
  evidence_threshold:
    type: statistical
    min_spans: 30                 # need a reasonable corpus before judging coverage
  thresholds:
    max_missing_fraction: 0.20    # flag if >20% of spans lack the field
    max_generic_fraction: 0.50    # flag if >50% of spans have no meaningful name
  inputs_required:
    traces: true
    config: optional              # sub-check 4 (untraced tools) needs config; others don't
  limitations: >
    Coverage thresholds are heuristics — a low-traffic or partially-instrumented
    agent may be flagged even when instrumentation is intentionally minimal. The
    untraced-tools sub-check can only run when config is available, and it cannot
    distinguish a tool that was never called from one that ran but emitted no span.
  remediation: >
    Adopt the OpenTelemetry GenAI semantic conventions so LLM and tool spans carry
    token counts, meaningful step names, and success/error status. Instrument every
    tool call. Emitting this telemetry both improves operability and unlocks the
    cost and context rules that depend on it.